// Корпус стража импортов: заведомо запрещённая ядру чужая библиотека.
// Каталог testdata компилятором не собирается — файл нужен только тесту
// TestImportGuard_Fires, который обязан на нём сработать.
package badcore

import "github.com/jackc/pgx/v5"

// Ссылка настоящая, а не blank-импорт: у blank соседние линтеры занимают
// строку, и запрет пропадает из находок (CONVENTIONS §1).
var _ = pgx.ErrNoRows
