# Хартия корректности: деньги, данные, время

Законы, обязательные для каждого пакета и адаптера тулкита. Это не стиль, а
условия, при которых у потребителя ничего не двоится, не теряется и не
всплывает через полгода жалобой клиента.

Каждый закон записан одинаково: **правило → почему → чем держится**. Держится —
это CHECK или UNIQUE в схеме, тест с известным именем, страж или guard-тест;
ревью и добрая воля стражем не считаются. **Закон, у которого не назван страж,
в пакете не реализован**: его место в списке «Чего в пакете НЕТ» с причиной, а
не в списке «Безопасность:».

Как пользоваться: чип пишет `doc.go`, где на каждый применимый закон стоит
инвариант с причиной и назван его страж; арбитр принимает по
[docs/CHIP.md](CHIP.md). Механику, которая эти законы исполняет, каталогизирует
[docs/PATTERNS.md](PATTERNS.md). Правила упаковки, схемы и денег в портах —
[CONVENTIONS.md](../CONVENTIONS.md) §9–10 и [ADR-0005](adr/0005-packaging.md);
хартия их не повторяет, а объясняет, от чего они защищают.

Примеры — по большей части из `mail`: это первый доведённый до тега пакет
тулкита и форма для остальных; где уместно, названы `kit`, `postgres` и
`scheduler`.

| # | Закон | Главный страж |
|---|---|---|
| 1 | [Деньги — целые минорные единицы](#1-деньги--целые-минорные-единицы) | `CHECK (amount_minor > 0)`, закрытый `Kind` рода записи |
| 2 | [Журнал append-only, сверка — вторая правда](#2-журнал-append-only-сверка--вторая-правда) | триггер неизменяемости, ноль `UPDATE` в адаптере |
| 3 | [Идемпотентность громкая](#3-идемпотентность-громкая) | `UNIQUE` по ключу, отпечаток, `ErrKeyReused` |
| 4 | [Арбитр — база, не Go](#4-арбитр--база-не-go) | `UNIQUE`/`CHECK`/`FOR UPDATE`, тест гонки |
| 5 | [Статусы — машина переходов](#5-статусы--машина-переходов) | CHECK ⊇ `All*`, CAS, тест позднего события |
| 6 | [Снапшот, а не ссылка](#6-снапшот-а-не-ссылка) | колонки снапшота NOT NULL, дайджест в отпечатке |
| 7 | [Атомарность — контракт порта](#7-атомарность--контракт-порта-доказанный-тестом) | `Test*_IsAtomic`, читающий из базы после отката |
| 8 | [Время — UTC и параметр](#8-время--utc-и-параметр) | `SetClock`, `scripts/timeguard.sh`, тесты под `TZ` не UTC |
| 9 | [Fail-closed](#9-fail-closed) | panic-валидация `Config`, `ErrUnavailable` → 503 |
| 10 | [Секреты и персональные данные](#10-секреты-и-персональные-данные) | `Sanitize`, закрытые метки, тест «секрета нет в тексте» |
| 11 | [Наблюдаемость — часть контракта](#11-наблюдаемость--часть-контракта) | `result` из трёх исходов, возраст старейшего pending |
| 12 | [Текст — побайтно и без мусора](#12-текст--побайтно-и-без-мусора) | `COLLATE "C"`, `scripts/sqlguard.sh`, валидаторы ядер |
| 13 | [Схема меняется на живой базе](#13-схема-меняется-на-живой-базе) | `SET LOCAL lock_timeout`, расширить → сузить, `scripts/sqlguard.sh` |
| 14 | [Ожидание и объём — с потолком](#14-ожидание-и-объём--с-потолком) | таймауты, `MaxBytesReader`, горутина с владельцем, `scripts/codeguard.sh` |
| 15 | [Антипаттерны](#15-антипаттерны) | — |

## 1. Деньги — целые минорные единицы

**Правило.** Сумма — `int64` в минорных единицах (копейки, центы) и всегда
положительная. Знак — не данные: род записи задаёт закрытый `Kind`
(`capture`, `refund`, …). Валюта едет рядом строкой и одна на весь расчёт:
складывать разные валюты нельзя. Округление — одно правило на продукт,
записанное в `doc.go`; распределение суммы по позициям детерминировано и без
потери копейки: Σ позиций == итог.

**Исключение — журнал движений `ledger`** ([ADR-0009](adr/0009-ledger.md),
уточнение 2): роды объявляет потребитель, и род «любой знак» делает знак
данными. Знак сверяется с родом в ядре и в схеме, а «сумма движений равна
остатку» остаётся простым суммированием.

**Почему.** `float64` не представляет 0.1 точно, и равенство сумм перестаёт
быть равенством. Отрицательная сумма позволяет «зачисление на минус» —
состояние, которого в домене нет, но которое проходит все проверки. Копейка,
потерянная при распределении скидки, всплывает в кассовом чеке, а не в тесте:
позиции не сойдутся с итогом, и чек отвергнет оператор фискальных данных.

**Чем держится.** `CHECK (amount_minor > 0)` и `CHECK (char_length(currency) = 3)`
в схеме; род записи — закрытый enum с CHECK и guard-тестом «CHECK ⊇ `All*`»;
на функцию распределения — property-тест «Σ частей == целое» и «нет
отрицательных» плюс gremlins; в портах `AmountMinor int64` и `Currency string`
([CONVENTIONS §10](../CONVENTIONS.md#10-деньги-и-транзакции-у-потребителя)),
поэтому чужой тип денег с чужим округлением в пакет не заезжает.

## 2. Журнал append-only, сверка — вторая правда

**Правило.** Записи денег и остатков только добавляются. Отмена, возврат,
списание — встречная запись со ссылкой на исходную, а не `UPDATE` и не
`DELETE`. Сумма встречных записей не превышает исходную. Сверка с внешней
правдой (выписка провайдера, его же API) — периодическая задача пакета, а не
разовый скрипт в чьём-то домашнем каталоге.

**Почему.** `UPDATE` строки журнала стирает историю: после него нельзя
доказать, что было списано и когда. «Исправить опечатку» и «замести
расхождение» выглядят в коде одинаково. Без сверки первым о расхождении
узнаёт клиент, а разбирать его приходится по логам, которых уже нет.

**Деньги и журналы действий различаются в одном.** У денежного журнала триггер
запрещает и `UPDATE`, и `DELETE`: удалённая строка книги — это потерянные
деньги. У журнала действий запрещается только `UPDATE`, потому что удаление
там — это ретеншн персональных данных, то есть обязанность потребителя перед
законом, а не попытка замести след. Правило одно и то же: историю нельзя
переписать; сколько её хранить, решает тот, кто отвечает за данные.

**Чем держится.** Триггер неизменяемости в миграции адаптера (`BEFORE UPDATE OR
DELETE` с `RAISE`; у журнала действий — только `UPDATE`), объявленный `ENABLE ALWAYS` — иначе он молчит для
владельца таблицы и при репликации; **у денежного журнала — ещё и
`BEFORE TRUNCATE ... FOR EACH STATEMENT`**, потому что построчный триггер
стирание таблицы целиком не ловит, а у журнала действий такого триггера
намеренно НЕТ: `DELETE` там разрешён под ретеншн, и запрет массового удаления
при разрешённом построчном означал бы запрет ретеншна ровно на том объёме, где
он и нужен. Замок ставится там, где дверь заперта; на приоткрытой он только
вводит в заблуждение того, кто будет читать схему через год; потолок встречных записей — триггер плюс
`FOR UPDATE` на родительской строке (две линии: одна против гонки, другая
против правки в обход адаптера); в коде адаптера нет ни одного `UPDATE` или
`DELETE` по таблице журнала, и это проверяется тестом по тексту SQL; сверка —
`Run(ctx) (int, error)` под `scheduler` с метрикой расхождения и алертом от
нуля.

## 3. Идемпотентность громкая

**Правило.** Ключ идемпотентности нормализуется одной функцией (обрезка
пробелов, регистр, потолок длины, пустой — ошибка). Тот же ключ и тот же
отпечаток параметров — прежний результат, отданный **как успех**. Тот же ключ
и другой отпечаток — ошибка (`ErrKeyReused`), а не тихий no-op и не второй
эффект. Ключ, уходящий провайдеру, производный от нашего идентификатора:
одинаковый между ретраями одной операции и разный между разными. Ключ не
освобождается после отказа — отказ тоже результат.

**Почему.** Две нормализации в двух местах — это два ключа: `" k"` и `"k"`
разъезжаются, и двойной клик становится двойным списанием. Тихий no-op на
изменившихся параметрах прячет ошибку вызывающего до момента, когда клиент
спросит, почему списали за другое. Случайный ключ провайдеру превращает
повтор неоднозначного таймаута во второй платёж.

**Нормализация обязана быть ИДЕМПОТЕНТНОЙ, и это проверяется фаззером, а не
обещанием в комментарии.** `f(f(x)) != f(x)` означает, что сохранённое и
найденное — разные строки: запись легла под одним ключом, а поиск идёт по
другому. Две ловушки, обе найдены прогоном и обе неочевидны:

- **приведение регистра выводит строку из нормальной формы Unicode.** У буквы
  с кольцом сверху нижний регистр даёт разложенную пару, которую NFKC собирает
  обратно, — то есть порядка «NFKC, потом регистр» НЕДОСТАТОЧНО, нормализовать
  надо дважды: `NFKC → регистр → NFKC`;
- **на негодном UTF-8 нормализация не идемпотентна вовсе**: оборванный ведущий
  байт «закрывает» собой следующую руну, та остаётся неразвёрнутой и
  разворачивается лишь на втором проходе. Отсюда `utf8.ValidString` до
  нормализации, а не после.

Там, где нормализованное значение — ещё и ключ счётчика попыток, у этого есть
второе дно: `Alice@x.ru` и `alice@x.ru` как два ключа означают, что потолок
попыток обходится клавишей Shift.

**Чем держится.** `UNIQUE` по ключу в схеме, имя индекса — контракт порта
([CONVENTIONS §9](../CONVENTIONS.md#9-схема-адаптера)); отпечаток канонический,
с префиксами длины у каждого поля — иначе `("ab","c")` и `("a","bc")` дают
один хэш; пустой сохранённый отпечаток совпадением не считается (страховка от
адаптера, потерявшего колонку); тест «тот же ключ, другое содержимое →
`ErrKeyReused`»; fuzz на нормализацию и на отпечаток.

Образец: [`mail/enqueue.go`](../mail/enqueue.go),
[`mail/envelope.go`](../mail/envelope.go),
[`mail/doc.go`](../mail/doc.go) п. 4.

## 4. Арбитр — база, не Go

**Правило.** Гонки решает Postgres: `UNIQUE`, `CHECK`, `FOR UPDATE`,
`FOR UPDATE SKIP LOCKED`. Проверить условие в Go и потом записать
(check-then-act) запрещено. Уникальность распознаётся по **имени
ограничения**, а не по коду `23505`. Конфликт внутри транзакции потребителя
разбирается через `ON CONFLICT … DO NOTHING`, а не перехватом ошибки.

**Почему.** Между `SELECT` и `INSERT` помещается чужая транзакция: под
нагрузкой «проверил — вставил» даёт два, и падает это в проде, а не в тесте.
Любой `23505` мог прийти от другого индекса — приняв его за дубль, пакет
похоронит настоящую ошибку. Ошибка Postgres переводит транзакцию потребителя
в aborted целиком: законный повтор письма уронил бы бизнес-факт, ради
которого письмо и пишется.

**Чем держится.** Индексы и CHECK в миграции адаптера; разбор конфликта по имени
([`postgres.IsUniqueViolation(err, constraint)`](../postgres/errors.go)) и
тест адаптера, проверяющий именно имя; тест гонки `Test*_Race`: N воркеров или
N параллельных транзакций — ровно один успех; тест «повтор внутри транзакции
потребителя её не роняет».

Образец: [`mail/mailpg/enqueue.go`](../mail/mailpg/enqueue.go) (комментарий
«`ON CONFLICT DO NOTHING`, А НЕ ПЕРЕХВАТ 23505»), контракт
`Store.Enqueue` в [`mail/ports.go`](../mail/ports.go).

## 5. Статусы — машина переходов

**Правило.** Набор статусов закрыт и зеркалится CHECK. Переходы описаны
таблицей в коде. Переход — CAS: `UPDATE … WHERE id = $1 AND status = $2`
(или `AND version = $3`), ноль затронутых строк — конфликт, а не успех.
Терминальные статусы не воскресают. Позднее событие про уже пройденное
состояние — дубль (успех) или конфликт (ошибка), но никогда не откат назад.

**Почему.** Провайдер присылает события повторно и не по порядку; поллинг и
вебхук приходят в любом порядке. Даунгрейд `succeeded → pending` возвращает
уже оплаченный заказ в подвешенное состояние и снимает выдачу. Статус строкой
вместо enum — это метка метрики с неограниченным набором значений и алерт по
строке, которой в коде уже нет.

**Чем держится.** `All*` со списком значений и guard-тест «CHECK ⊇ `All*`»;
таблица переходов с табличным тестом на каждый запрещённый переход; CAS,
трактующий ноль строк как конфликт; тест «поздний `pending` после
терминального статуса не меняет строку».

Образец: статусы `pending → sending → sent | failed | expired | suppressed` в
[`mail/envelope.go`](../mail/envelope.go), CHECK в
[`mail/mailpg/migrations/00001_mail_init.sql`](../mail/mailpg/migrations/00001_mail_init.sql), `Finish` обновляет
только строку в `sending` — ноль строк даёт `ErrUnavailable`
([`mail/mailpg/finish.go`](../mail/mailpg/finish.go)).

## 6. Снапшот, а не ссылка

**Правило.** Цена, срок, состав, адрес, отправитель — замораживаются в момент
сделки в самой записи. После сделки каталог не перечитывается: то, за что
заплачено, известно из строки, а не из `JOIN`.

**Почему.** Цена изменилась — и сумма возврата перестала совпадать с суммой
платежа. Позицию удалили из каталога — заказ невозможно показать и невозможно
вернуть. `JOIN` на изменяемый справочник делает историю функцией сегодняшнего
дня, а спор с клиентом — неразрешимым.

**Чем держится.** Колонки снапшота `NOT NULL` в схеме сделки; ссылка на
каталог — идентификатором, без FK на таблицу потребителя
([CONVENTIONS §9](../CONVENTIONS.md#9-схема-адаптера)); дайджест состава
входит в отпечаток идемпотентности, поэтому тот же ключ на другой состав даёт
конфликт (закон 3); тест «изменили справочник — запись сделки не изменилась».

Образец: `From` попадает в конверт снимком из `Config`
([`mail/envelope.go`](../mail/envelope.go)), а не перечитывается при отправке;
`NotAfter` фиксируется при постановке в очередь.

## 7. Атомарность — контракт порта, доказанный тестом

**Правило.** «В одной транзакции» — обещание, которое компилятор не проверяет.
Каждый pg-адаптер даёт `WithTx(tx)`; каждый такой контракт доказывается
интеграционным тестом `Test*_IsAtomic`, который **после отката читает из
базы** и убеждается, что нет ни бизнес-факта, ни строки дедупа. Там, где
двойной эффект стоит денег, третья линия — частичный уникальный индекс
(«одно живое намерение на заказ»).

**Почему.** Вставка мимо транзакции компилируется, проходит тесты и теряет
данные ровно так же, как код без outbox вообще: это самая частая ошибка
внедрения. Тест, проверяющий только «вернулась ошибка», зелен и на адаптере,
который уже вставил строку отдельным соединением, — а такой адаптер оставляет
ключ занятым после отката, и следующий законный повтор операции у потребителя
будет молча отвергнут.

**Чем держится.** `Test*_IsAtomic`, читающий из базы после `ROLLBACK`, включая
строку дедупа
([CONVENTIONS §5](../CONVENTIONS.md#5-тесты)); сигнатура адаптера
`New(pool)` / `WithTx(tx)` / `Querier`; частичный `UNIQUE … WHERE`; для
очередей — тест краха между внешним эффектом и записью исхода
([PATTERNS §1](PATTERNS.md#1-транзакционный-outbox-и-enqueue-только-в-транзакции)).

Образец: `TestStore_WithTx_IsAtomic` в
[`mail/mailpg/enqueue_test.go`](../mail/mailpg/enqueue_test.go),
пункт 2 списка «Безопасность:» в
[`mail/mailpg/doc.go`](../mail/mailpg/doc.go).

## 8. Время — UTC и параметр

**Правило.**

1. **Момент — `time.Time` в UTC.** Часы — поле сервиса
   `func() time.Time { return time.Now().UTC() }`, подменяемое `SetClock`
   (паника на nil). Прямой `time.Now()` в коде прода — только для
   длительности через `time.Since`: момент, который куда-то пишется,
   берётся из часов.
2. **Внешнее приводится к UTC там, где пришло.** Адаптер — сразу после
   чтения из базы: pgx отдаёт `timestamptz` в поясе процесса, если не задан
   `ScanLocation`. Разбор заголовка, тела вебхука, ответа провайдера и
   конфига — там же. Момент без пояса снаружи не принимается, а чужая отметка
   на веру не берётся: она зажимается в допустимое окно или заменяется
   нашей.
3. **Хранится только `timestamptz`, время — параметром.** `timestamp` без
   пояса, `now()`, `current_timestamp` и родня в схеме и запросах запрещены;
   соединение пинуется в UTC (`postgres.WithUTC`).
4. **Точность — микросекунды, сравнение — `Equal`.** Момент, который пройдёт
   через Postgres, усекается до микросекунд до сравнения, хэша и подписи.
   `==` моментов не сравнивает: он сравнивает ещё пояс и показания монотонных
   часов, и то же мгновение после круга через базу «не равно».
5. **Бизнес-календарь — в явном поясе из конфига.** «Сутки по Москве», «до
   конца месяца», «рассылка в 10:00» считаются в `*time.Location` из
   `Config`, загруженном на старте; `time.Local` в коде не бывает. Арифметика
   дат — в этом поясе, в хранилище и порты — снова UTC.
6. **Процесс не зависит от пояса машины.** Время в логах и ответах наружу —
   UTC, выставленное кодом (RFC 3339 с `Z`), а `TZ=UTC` контейнера — второй
   рубеж, а не первый.

**Почему.** Без `SetClock` тест на просрочку вынужден спать, а результат
зависит от часов машины — и заодно делает недетерминированным прогон
мутационного тестирования. `DEFAULT now()` — вторая правда о времени: тест
проверяет управляемые часы, а база пишет свои. `timestamp` без пояса хранит
показание стенных часов, и при смене пояса сервера или соединения мгновение
теряется молча. Момент в поясе процесса — то же мгновение, но в логе контейнера
с `TZ=Europe/Moscow` он `+03:00`, записи двух сервисов не сходятся по времени,
а текст, склеенный из такого момента без `.UTC()`, расходится с подписью.
Арифметика дат в поясе процесса даёт сутки в 23 и 25 часов на переводе часов.
Чужая отметка в будущем прячет строку от выборки «пора делать», в прошлом —
делает её вечно просроченной.

**Чем держится.**

- [`scripts/timeguard.sh`](../scripts/timeguard.sh) — джоб CI `guards`,
  `make timeguard` и шаг `make chip-check`: в миграциях нет `timestamp` без
  пояса и функций времени, в коде прода — `time.Local`, `.Local()` и
  `time.Now` мимо часов в UTC. Исключения — с причиной в
  [`scripts/timeguard.allow`](../scripts/timeguard.allow); запись, которой
  больше нет в коде, роняет стража.
- Тесты CI, `make ci` и `make chip-check` идут под `TZ=America/St_Johns` —
  получасовое смещение и перевод часов: код, тайком зависящий от пояса
  машины, падает. 2026-09-16 весь репозиторий под этим поясом зелёный.
- Контрактные наборы `mailtest`, `outboxtest`, `entitlementtest`,
  `ledgertest` и `inboxtest` передают моменты в чужом поясе с наносекундами и
  сверяют `Location() == time.UTC` и микросекунды — и у двойника, и у
  адаптера.
- `TestWithUTC_BeatsServerTimezone` в `postgres`: пин соединения бьёт пояс
  сервера. Тесты ядра на управляемых часах — критерий готовности 5
  ([ADR-0005](adr/0005-packaging.md#критерии-готовности-пакета-к-тегу-v010));
  усечение [`pgtest.Now`](../postgres/pgtest/helpers.go); тест на границы
  окна для чужих отметок.

Образец: `SetClock` и часы в UTC в [`mail/service.go`](../mail/service.go);
все времена конверта назначает сервис, схема их не подставляет. Правила для
проекта — контейнер, логи, бизнес-пояс — [CONSUMER §8](CONSUMER.md#8-время-и-окружение).

## 9. Fail-closed

**Правило.** Нулевое значение конфига — отказ, а не «выключено». Nil-порт и
негодный `Config` — паника на старте, текстом вида `Config.X must …`. Сбой
хранилища — `ErrUnavailable` → 503, никогда не 403 «прав нет» и никогда не
«пропустим». Флага, выключающего инвариант (`SkipSignature`, `TrustClient`,
`SendSynchronously`), не существует: режим разработки достигается подменой
порта.

**Почему.** «Выключено по умолчанию» означает, что забытая переменная
окружения тихо снимает защиту в проде — и никто этого не заметит, потому что
всё работает. 403 при недоступной базе учит поддержку и клиента неверному
диагнозу: чинить будут права, а не базу. Флаг, выключающий инвариант, рано
или поздно окажется включённым в проде, и это будет не баг, а разрешённая
конфигурация.

**Чем держится.** Panic-валидация в конструкторе и табличный тест на каждое
поле `Config`; класс ошибки `unavailable → 503` в единой точке ошибок
([`kit/errs`](../kit/errs/kind.go), [`kit/errs/httperr`](../kit/errs/httperr/responder.go));
отсутствие `Skip*`-полей проверяется на приёмке; вместо флага — двойник или
явный «ненастроенный» порт.

Образец: [`mail/config.go`](../mail/config.go) (тексты `Config.Lease must be
longer than Config.SendTimeout` и прочие),
[`mail/unconfigured.go`](../mail/unconfigured.go) — прод без провайдера как
транспорт, а не как флаг и не как логирование секретов.

## 10. Секреты и персональные данные

**Правило.** Токены, ключи, тела сообщений и ссылки с токенами не попадают в
логи, в тексты ошибок и в метки метрик. `PgError.Detail` до ошибки не
доходит: от Postgres остаются SQLSTATE и Message. Метки метрик — закрытые
наборы; идентификаторы, адреса и тексты ошибок метками не становятся.
Персональные данные в payload очередей живут под ретеншном и правилами
152-ФЗ; удаление аккаунта — анонимизация, если журналы и заказы держат ссылку
`RESTRICT`.

**Почему.** В `Detail` Postgres кладёт «Failing row contains (…)» — всю
строку целиком, вместе с телом письма и ссылкой с токеном. Лог — самое
доступное место системы, и он переживает инцидент дольше всех. Метка с
адресом взрывает кардинальность и уносит персональные данные в систему
мониторинга, откуда их уже не удалить по требованию субъекта. Каскадное
удаление аккаунта уничтожает финансовую историю, которую обязаны хранить.

**Чем держится.** [`postgres.Sanitize`](../postgres/errors.go) на границе
каждого адаптера; тест, ищущий подстроку секрета в тексте ошибки
([ADR-0005](adr/0005-packaging.md#критерии-готовности-пакета-к-тегу-v010),
п. 9); закрытые enum для меток с guard-тестом; CHECK, не принимающий
терминальную строку с непустым телом; FK `RESTRICT` и контракт анонимизации;
ретеншн в `Config`, валидируемый снизу.

Образец: [`mail/mailpg/errors.go`](../mail/mailpg/errors.go) и пункт 1
списка «Безопасность:» в [`mail/mailpg/doc.go`](../mail/mailpg/doc.go);
`email_outbox_body_cleared_chk` в
[`mail/mailpg/migrations/00001_mail_init.sql`](../mail/mailpg/migrations/00001_mail_init.sql) — тело стирает база, а
не только код.

## 11. Наблюдаемость — часть контракта

**Правило.** В счётчике внешних вызовов `result` различает три исхода:
`ok` / `rejected` (внешняя сторона ответила определённо «нет») / `error`
(ответа нет, повтор осмыслен). Фоновая очередь измеряется **возрастом
старейшей необработанной записи**, а не глубиной. Терминальный отказ
(dead-letter) виден, ретеншном не чистится, имеет алерт от нуля и
разбирается руками; повторный запуск — операторское действие с аудитом, а не
`UPDATE` в консоли.

**Почему.** Сведи `rejected` и `error` — и алерт «провайдер недоступен» будет
гореть от фонового шума отказов, а потом его выключат. Глубина очереди мала и
при полностью вставшей доставке: пять строк, которые не уходят третий час,
выглядят здоровее тысячи, которые уходят. Молчаливый вечный ретрай
превращает очередь в мусорку, о которой узнают через полгода.

**Чем держится.** Закрытые наборы меток с guard-тестом; `Stats` со снимком и
гейджи через `Set` — запроса в базу на каждый scrape в пакете нет
([CONVENTIONS §6](../CONVENTIONS.md#6-наблюдаемость)); тест «`Purge` не трогает
`failed`»; таблица «метрика — тип — что» и пороги алертов в `doc.go` или ADR —
пункт 12 критериев готовности.

Образец: `Stats` в [`mail/ports.go`](../mail/ports.go), гейджи в
[`mail/mailotel/gauges.go`](../mail/mailotel/gauges.go), таблица алертов —
[ADR-0001](adr/0001-mail.md#наблюдаемость-и-алерты-для-таблиц-потребителя).

## 12. Текст — побайтно и без мусора

**Правило.**

1. **Текстовая колонка — `COLLATE "C"`.** В схеме блока сортировка `"C"` у
   каждой колонки `text`, `varchar`, `char`: порядок, сравнение `<` и `>`,
   `LIKE 'префикс%'` и индексы идут по байтам — так же, как `sort.Strings`,
   `bytes.Compare` и двойники. В схеме проекта так же объявляются ключи,
   коды, слаги, идентификаторы и адреса, по которым ищут точным совпадением.
   Человеческий текст, который сортируют и ищут для людей, — имена,
   названия — держит сортировку языка явно (`COLLATE "ru-RU-x-icu"`): в `"C"`
   `lower()`, `upper()` и `ILIKE` кириллицу не сворачивают.
2. **Текст на входе проверяет ядро, а не база.** Строка, которая уйдёт в
   колонку, метку метрики или заголовок, — валидный UTF-8 без `\x00` и
   управляющих символов (перевод строки — только там, где форма поля его
   разрешает явно), длина — в байтах и с потолком. Отказ — класс «неверный
   ввод», а не сбой хранилища.
3. **Нормализация — одна точка.** Строку, по которой ищут и сравнивают
   (логин, адрес как ключ), одна функция приводит к форме до записи и до
   поиска; негодный UTF-8 отвергается, а не чинится (образец — `auth/loginid`).

**Почему.** Сортировка по умолчанию берётся из локали сервера базы. При одной
и той же объявленной `en_US.utf8` образ `postgres:16-alpine` (musl) сортирует
побайтно, а `postgres:16` на Debian (glibc) — по правилам языка. Проверено
2026-09-17 на обоих образах: `Evt3 < evt-2 < evt10 < evt9 < evt_1` и
`evt_1 < evt10 < evt-2 < Evt3 < evt9`. Тесты тулкита идут на alpine, прод —
где угодно: выборка «первые N по ключу», курсор и уборка равных моментов
«по ключу» у адаптера на проде берут не те строки, что у двойника, а тесты
зелёные. Хуже того, обновление glibc меняет правила сравнения под живым
индексом: B-tree на текстовом ключе становится неверным, и уникальный ключ
дедупа пропускает дубль, пока индекс не перестроят. У `"C"` правила не
меняются никогда. Postgres не хранит `\x00` в `text` и отвергает негодный
UTF-8 ошибкой запроса: без проверки в ядре ввод клиента становится 500 и
алертом «база сломалась», а в транзакции потребителя — прерванной транзакцией.

**Чем держится.**

- [`scripts/sqlguard.sh`](../scripts/sqlguard.sh) — джоб CI `guards`,
  `make sqlguard` и шаг `make chip-check`: текстовая колонка миграции без
  `COLLATE "C"` сразу после типа — отказ. Исключения — с причиной в
  [`scripts/sqlguard.allow`](../scripts/sqlguard.allow): там выпущенные первые
  миграции модулей под тегами, их колонки переводит новая миграция после
  тегов ([ROADMAP](ROADMAP.md), «Признанные долги»).
- Валидаторы ядер — `ValidEventID`, `ParseKey`, `auth/loginid`, проверки
  подробностей `audit` и полей `outbox`, `mail`, `ledger`, `payment` — и
  контрактные наборы, которые гонят через ядро и через базу одни и те же
  значения (образец — CHECK против валидаторов «по каждому байту ASCII» в
  `inboxpg`).

## 13. Схема меняется на живой базе

**Правило.**

1. **Миграция не ждёт блокировку дольше секунд.** Первый оператор каждого
   раздела `Up` и `Down` — `SET LOCAL lock_timeout = '5s'`. В файле
   `-- +goose NO TRANSACTION` — `SET lock_timeout` в начале раздела и
   `RESET lock_timeout` в конце: `SET LOCAL` вне транзакции не действует.
2. **Изменение совместимо с предыдущей версией кода: расширить → перевести
   код → сузить.** На выкате старые и новые реплики работают с одной схемой.
   Колонку не переименовывают и не удаляют в том выпуске, где код перестал её
   читать; новая колонка — без `NOT NULL` или с постоянным `DEFAULT`;
   удаление и `NOT NULL` — следующим выпуском.
3. **Большая таблица не переписывается и не сканируется под блокировкой.** В
   миграции после первой индекс — `CREATE INDEX CONCURRENTLY` отдельным файлом
   без транзакции; `CHECK` и внешний ключ — с `NOT VALID`, затем
   `VALIDATE CONSTRAINT` отдельным шагом. Смена типа колонки и `SET NOT NULL` —
   только с записанной причиной, почему это допустимо для этой таблицы.

**Почему.** `ALTER TABLE` берёт `ACCESS EXCLUSIVE`. Ждущая её миграция стоит в
очереди блокировок первой и держит за собой все последующие запросы к таблице,
даже чтения: одна долгая транзакция отчёта превращает выкат в простой. С
`lock_timeout` миграция падает и повторяется, а прод работает. Переименованная
колонка ломает реплики старой версии на те минуты выката, что они ещё живы, —
500 без единой правки их кода. `CREATE INDEX` без `CONCURRENTLY` запрещает
запись в таблицу на всё время построения, проверка нового ограничения
сканирует таблицу под блокировкой.

**Чем держится.**

- [`scripts/sqlguard.sh`](../scripts/sqlguard.sh): первый оператор раздела не
  `SET LOCAL lock_timeout` — отказ; в миграции после первой переименование,
  удаление колонки или таблицы, смена типа, `SET NOT NULL`, индекс без
  `CONCURRENTLY` и ограничение без `NOT VALID` — отказ. Исключение — только
  с причиной в `scripts/sqlguard.allow`.
- Тесты миграций адаптера ([ADR-0011](adr/0011-migrations-in-blocks.md)) —
  накат, двойной откат и повторный накат на живой базе.
- Раннер и выкат проекта — [CONSUMER §6](CONSUMER.md#6-миграции).

## 14. Ожидание и объём — с потолком

**Правило.**

1. **У всего, что ждёт, — срок.** HTTP-клиент — `http.Client` с `Timeout`;
   у блока это умолчание, которое `Config` может заменить своим клиентом, а
   `http.DefaultClient`, `http.Get` и родня запрещены. HTTP-сервер — все
   четыре срока: `ReadHeaderTimeout`, `ReadTimeout`, `WriteTimeout`,
   `IdleTimeout`; ручке, которой нужно дольше, срок продлевает
   `http.ResponseController`, а не сервер целиком. Запрос к базе —
   `lock_timeout` и `statement_timeout` внутри транзакции (`postgres.Runner`);
   транзакция, забытая открытой, — `idle_in_transaction_session_timeout` у
   пула. Остальное ожидание в коде — через `ctx` со сроком выше по стеку.
2. **У всего, что читается, — потолок.** Тело запроса — `http.MaxBytesReader`,
   ответ чужого API — `io.LimitReader` до разбора, выборка множества — `LIMIT`
   параметром, пачка и очередь — размер в `Config`. Превышение — отказ с
   классом, а не молчаливая обрезка.
3. **У горутины — владелец.** Каждая горутина прода восстанавливается из
   паники (`recover`), выходит по `ctx` или сигналу остановки, и её ждёт тот,
   кто её запустил (`WaitGroup`, канал). Фоновая работа блока — задача
   планировщика `Run(ctx) (int, error)`, а не своя горутина.

**Почему.** Без срока один зависший провайдер держит соединение, горутину и
слот пула, пока ядро не закроет TCP, — это минуты и часы. Запросы копятся, пул
кончается, сервис стоит, а ошибок в метриках нет: ни один запрос ещё не
закончился. Сервер без `ReadTimeout` отдаёт соединение медленному телу, без
`IdleTimeout` — простаивающему keep-alive. Забытая открытой транзакция держит
блокировки и не даёт `VACUUM` убрать строки. Тело без потолка отдаёт память
процесса клиенту. Паника в горутине без `recover` завершает весь процесс
вместе с недописанными транзакциями соседей, и обработчик паник HTTP её не
видит.

**Чем держится.**

- [`scripts/codeguard.sh`](../scripts/codeguard.sh) — джоб CI `guards`,
  `make codeguard` и шаг `make chip-check`: `http.DefaultClient` и его обёртки,
  литерал `http.Client` без `Timeout`, литерал `http.Server` без любого из
  четырёх сроков, чтение `.Body` без `io.LimitReader` или
  `http.MaxBytesReader`, любой оператор `go` — отказ. Исключение — запись с
  причиной в [`scripts/codeguard.allow`](../scripts/codeguard.allow); у
  горутины причина называет, где её `recover` и кто её ждёт.
- `postgres.Runner` ставит `SET LOCAL` сроки в каждой транзакции (тесты
  `postgres`); параметры пула и серверов проекта — [CONSUMER §9](CONSUMER.md#9-сеть-база-и-json).

## 15. Антипаттерны

| Приём | Почему запрещён | Закон |
|---|---|---|
| `float64` (или строка) для денег | 0.1 + 0.2 ≠ 0.3; равенство сумм перестаёт быть равенством, а ошибка накапливается по позициям | 1 |
| `UPDATE` строки журнала, даже «поправить опечатку» | стирает историю; правка — встречная запись со ссылкой | 2 |
| Повтор внешнего вызова без ключа идемпотентности | неоднозначный таймаут превращается во второе списание или второе письмо | 3 |
| Тихий no-op на повторе с другими параметрами | прячет ошибку вызывающего до жалобы клиента | 3 |
| «Проверил в Go — записал» (check-then-act) | между проверкой и записью помещается чужая транзакция | 4 |
| Перехват любого `23505` как «дубль» | ошибка другого индекса будет принята за законный повтор | 4 |
| `TRUNCATE` или `DELETE FROM` в тестах на общей базе | тест роняет чужие данные, а зелёный прогон ничего не доказывает; изоляция — своя схема или база на прогон | 7 |
| `time.Now()` в ядре | тест на просрочку вынужден спать, поведение зависит от часов машины, прогон мутантов недетерминирован | 8 |
| `DEFAULT now()` в доменной колонке | вторая правда о времени: тест проверяет одно, база пишет другое | 8 |
| Разбор тела вебхука до проверки подписи (а если подписи нет — до перечитывания объекта у провайдера) | незнакомец тратит наше соединение из пула и диктует состояние | 9 |
| Флаг, выключающий инвариант (`Skip*`, `Trust*`) | однажды окажется включённым в проде, и это будет разрешённая конфигурация | 9 |
| `http.Error` мимо единой точки ошибок | ответ без слага и идентификатора запроса, статус мимо таблицы и лог не той громкости; держится guard-тестом [`errstest`](../kit/errs/errstest/guards.go) | 9 |
| `PgError.Detail` в тексте ошибки | в нём лежит вся строка целиком — с телом сообщения и токеном | 10 |
| Автомиграция схемы из библиотеки на старте | две правды о схеме, DDL-права у приложения, гонка реплик при выкате; библиотека даёт миграции (`Migrations()`) и `CheckSchema`, накатывает раннер проекта | 10 |
| Гейдж глубины очереди вместо возраста старейшей записи | полностью вставшая доставка выглядит здоровой | 11 |
| Вечный молчаливый ретрай без dead-letter | очередь становится мусоркой, о которой узнают через полгода | 11 |
| Текстовый ключ в сортировке языка (сортировка базы по умолчанию) | порядок зависит от ОС сервера, alpine и glibc сортируют по-разному, а обновление glibc портит индекс под уникальным ключом | 12 |
| `lower()` или `ILIKE` по колонке `"C"` для кириллицы | в `"C"` регистр сворачивается только у ASCII: поиск молча не находит | 12 |
| Проверка ввода «на совести базы» | `\x00` и негодный UTF-8 приходят ошибкой запроса: 500 и прерванная транзакция вместо 400 | 12 |
| Переименование или удаление колонки в выпуске, где код перестал её читать | реплики старой версии на выкате получают 500 | 13 |
| `CREATE INDEX` без `CONCURRENTLY` и ограничение без `NOT VALID` на живой таблице | запись в таблицу стоит всё время построения или проверки | 13 |
| `http.DefaultClient`, сервер без `ReadTimeout` и `IdleTimeout` | зависший провайдер или медленный клиент выбирает пул и соединения, а метрики ошибок молчат | 14 |
| `io.ReadAll(r.Body)` без потолка | память процесса в руках клиента | 14 |
| `go func()` без `recover` и без того, кто её ждёт | паника в ней завершает весь процесс, остановка её теряет | 14 |
