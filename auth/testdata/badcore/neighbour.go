// Второй корпус стража: импорт СОСЕДНЕГО модуля тулкита. ADR-0005 разрешает
// внутри rebar ровно две зависимости — на kit и (из _test.go) на
// postgres/pgtest, — плюс третью, на postgres, но только адаптерам хранилища.
// Ядро auth ни под одну не подпадает, и страж обязан это ловить: запрет на
// драйвер тут не сработает, импорт выглядит «своим».
package badcore

import "github.com/nrect/rebar/postgres"

var _ = postgres.Sanitize
