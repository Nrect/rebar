// Package inboxhttp — HTTP-ручка приёма inbox: потолок тела, адрес от
// периметра, подтверждение источника, отказ классом ошибки (ADR-0012,
// решение 7).
//
// Безопасность:
//
//  1. ОТВЕТ КЛАССОМ, БЕЗ СЛОВАРЯ ПРОДУКТА: Translate потребителя не
//     применяется — правило, совпавшее с ошибкой обработчика под
//     inbox.ErrUnavailable, превратило бы 503 в 4xx.
//  2. ТЕЛО ЧИТАЕТСЯ НЕ ДАЛЬШЕ ПОТОЛКА ПЛЮС БАЙТ: подпись не считается по
//     неограниченному входу, а too_large считает сервис.
//  3. АДРЕС — ФУНКЦИЕЙ ПЕРИМЕТРА: Config.RemoteIP обязателен, r.RemoteAddr за
//     прокси — адрес прокси.
//  4. ПОДТВЕРЖДЕНИЕ — РОВНО 200, 410 НЕ ОТДАЁТСЯ НИКОГДА: по Standard Webhooks
//     410 — просьба отключить приёмник.
//  5. ТЕЛО И ЗАГОЛОВКИ В ЛОГ НЕ ИДУТ: лог ошибки пишет httperr с текстом
//     ошибки, а ошибки ядра их не цитируют.
//
// # Чего в пакете НЕТ
//
//  1. МАРШРУТА И МЕТОДА: проект монтирует ручку на POST своего пути.
//  2. TLS, ЛИМИТА ЗАПРОСОВ И СЕТЕЙ НА ПЕРИМЕТРЕ: у проекта.
//  3. RETRY-AFTER НА in_flight: отправитель повторяет по своему расписанию.
package inboxhttp
